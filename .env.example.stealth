# Example: Stealth Mode Configuration
# This demonstrates using the ENABLE_STEALTH macro for anti-detection

# ============================================================================
# STEALTH MODE - Simple Configuration
# ============================================================================

# Enable stealth mode with a single setting!
# This automatically configures:
#   - init_script: Path to bundled stealth.js
#   - headless: false (headed mode for better stealth)
#   - user_agent: Recent Chrome user agent
PW_MCP_PROXY_ENABLE_STEALTH=true

# Optional: Override individual stealth settings if needed
# PW_MCP_PROXY_HEADLESS=true  # Uncomment to force headless mode
# PW_MCP_PROXY_USER_AGENT=Mozilla/5.0 (Custom)  # Uncomment for custom UA

# ============================================================================
# Browser Pool Configuration
# ============================================================================

# Define a single pool with stealth enabled
PW_MCP_PROXY__DEFAULT_INSTANCES=1
PW_MCP_PROXY__DEFAULT_IS_DEFAULT=true
PW_MCP_PROXY__DEFAULT_DESCRIPTION=Stealth-enabled browser pool

# ============================================================================
# Additional Stealth-Enhancing Settings (Optional)
# ============================================================================

# Use persistent browser profile for cookies/cache (more realistic)
# PW_MCP_PROXY_USER_DATA_DIR=/app/browser-profile
# PW_MCP_PROXY_ISOLATED=false
# PW_MCP_PROXY_SAVE_SESSION=true

# Use realistic viewport
PW_MCP_PROXY_VIEWPORT_SIZE=1920x1080

# Optional: Use proxy for IP rotation
# PW_MCP_PROXY_PROXY_SERVER=http://proxy.example.com:8080

# ============================================================================
# Blob Storage (Default Values)
# ============================================================================

BLOB_STORAGE_ROOT=/mnt/blob-storage
BLOB_MAX_SIZE_MB=500
BLOB_TTL_HOURS=24
BLOB_SIZE_THRESHOLD_KB=50
BLOB_CLEANUP_INTERVAL_MINUTES=60
